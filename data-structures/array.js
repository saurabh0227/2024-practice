const arr = [1, 2, 3, 'Saurabh'];
arr.push(4); // To add element at the end of array
arr.unshift(0); // To add element at the start of array
arr.pop(); // To remove form last
arr.shift(); // To remove form start
console.log(arr[0]);

for (const item of arr) {
  confirm.log(item);
}

// map, filter, reduce, concat, slice and splice
